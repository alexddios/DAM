# 📱 UT1: Del JavaScript que ja coneixem al desenvolupament mòbil

**Módulo:** PMDM | **Curso:** 2n DAM
**Stack:** JavaScript, Vite, React, Expo
**Tags:** #PMDM #React #ReactNative #DesarrolloMóvil #Vite

---

## 🛠️ 1. Metodología de trabajo
- Explicación corta ➡️Lo vemos funcionar ➡️ Lo probamos nosotros ➡️ Lo complicamos un poco ➡️ **Sprint 1**
- Las prácticas sirven para aprender; el Sprint para demostrar qué sabes hacer.

## 📱 2. Contexto: ¿Qué significa desarrollar para móvil?
- **Desarrollo Nativo:**
  - Android ➡️ Kotlin / Java
  - iOS ➡️ Swift
  - *Ventaja:* Máximo control de cada plataforma.
- **Desarrollo Multiplataforma (DAM):**
  - Una base de código compartida.
  - *Objetivo:* Desplegar en Android + iOS con el mismo código. React Native es una de las opciones principales.

## ⚛️ 3. ¿Por qué React? (Del conocimiento previo al nuevo)
- **JavaScript:** Ya lo conocemos de 1º.
- **Componentes:** Construimos piezas pequeñas.
- **Composición:** Las piezas forman interfaces completas.
- **Reutilización:** Escribimos una vez, reutilizamos en múltiples lugares.
> **Idea central:** No queremos memorizar React, queremos entender su modelo mental.

## 🧠 4. Idea Importante: React no es React Native
El modelo mental es el mismo, pero el destino cambia:
- **[[React]] (Core):** Componentes, props, estado, hooks.
  - ⬇️ **React DOM:** Interfaces web (Navegador).
  - ⬇️ **[[React Native]]:** Interfaces móviles (Android + iOS).
*Primero entenderemos React. Después llevaremos el mismo modelo al móvil.*

## 🧰 5. Herramientas (Nuestro Stack)
- **VS Code:** Editor de código.
- **Node.js:** Runtime.
- **npm:** Gestor de paquetes.
- **Vite:** Empaquetador para arrancar el proyecto React.
- **React:** Librería de componentes.
- **Expo:** Entorno y herramientas para React Native.

## 🚀 6. Hands-on: Primer proyecto React con Vite
Para arrancar un proyecto y que aparezca algo en pantalla lo antes posible:
```bash
npm create vite   # Crear proyecto (Elegir React + JavaScript)
npm install       # Instalar dependencias
npm dev           # Arrancar el servidor de desarrollo
```
## 📁 7. Estructura del proyecto Vite (Orientación inicial)

- `package.json`: Dependencias y scripts del proyecto.
    
- `src/main.jsx`: Punto de entrada de la aplicación.
    
- `src/App.jsx`: Nuestro componente principal.
    
- Carpetas extra: `src/`, `public/`, `node_modules/`.
    

## 🧩 8. JSX (JavaScript XML)

Parece HTML... pero no lo es. Nos permite describir la interfaz visual directamente dentro de JavaScript.

- Las llaves `{ ... }` permiten introducir expresiones de JavaScript dinámicas.
```js
const nom = "Miquel";

function App() {
  return (
    <div>
      <h1>Hola, {nom}!</h1>
    </div>
  );
}
```
## 🧱 9. La idea clave de React: El Componente
Un componente es simplemente una función que devuelve una porción de interfaz. Nos permite crear nuestras propias etiquetas reutilizables.
```js
function StudentCard() {
  return (
    <div>
      <h2>Miquel</h2>
      <p>2n DAM</p>
    </div>
  );
}
```
**Uso:** Al crear este componente, ahora podemos usar la etiqueta `<StudentCard />` todas las veces que queramos, creando instancias independientes.